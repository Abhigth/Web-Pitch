---
phase: 03-email-engine
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified: [src/app/dashboard/campaigns/page.tsx]
autonomous: true
requirements: [CORE-06]
must_haves:
  truths:
    - Users can trigger email sending for scraped campaigns
    - The server enqueues all unprocessed leads into the email worker
  tests:
    - Build passes
---

## Objective
Provide the UI hook for users to review their structured leads and officially "Start Sending" the campaign, bridging the frontend to the pacing engine.

## Tasks

<task type="auto">
  <name>Task 1: Campaign Push Action</name>
  <files>src/app/dashboard/campaigns/page.tsx</files>
  <read_first>src/app/dashboard/campaigns/page.tsx, src/lib/queue.ts</read_first>
  <action>
    - Add a new server action `startSendingCampaign(campaignId: string)` in `src/app/dashboard/campaigns/page.tsx`.
    - Inside, `const leads = await prisma.lead.findMany({ where: { campaignId, status: 'NEW' } })`.
    - Fetch the default template (we'll just use a hardcoded default string or fetch a template if available).
    - `import { emailQueue } from "@/lib/queue"`
    - Loop through `leads` and enqueue jobs: `await emailQueue.add('sendEmail', { leadId: l.id, templateText: "Hi {name}, I noticed you don't have a website. Can I build you one?" })`.
    - Update campaign status: `await prisma.campaign.update({ where: { id: campaignId }, data: { status: 'SENDING' } })`.
    - Under the existing mapping of `campaigns` in the render tree, if `c.status === 'COMPLETED'`, render a "Start Sending" Button that wraps the server action `startSendingCampaign.bind(null, c.id)`.
  </action>
  <acceptance_criteria>
    - `startSendingCampaign` extracts leads mapped to the campaign
    - `emailQueue.add` receives the leads array iteratively
    - A visible "Start Sending" button exists for `COMPLETED` campaigns
  </acceptance_criteria>
</task>
