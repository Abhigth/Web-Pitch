---
phase: 03-email-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/app/dashboard/settings/page.tsx, src/lib/actions.ts]
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - User can configure their SMTP or Gmail App Password credentials
  tests:
    - Build passes
---

## Objective
Enable users to securely configure their existing SMTP or Gmail credentials for outgoing cold-email pipelines.

## Tasks

<task type="auto">
  <name>Task 1: Prisma Schema Settings Update</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
    - Add a new model `SmtpSettings`.
    - Fields: `id` (String @id @default(cuid())), `userId` (String @unique), `host` (String), `port` (Int), `userString` (String), `passString` (String).
    - Add relation `user User @relation(fields: [userId], references: [id], onDelete: Cascade)`.
    - Also add `smtpSettings SmtpSettings?` to the existing `User` model.
    - Important: Do not run migration in plans; just edit the schema.
  </action>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model SmtpSettings`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Settings UI and Server Action</name>
  <files>src/app/dashboard/settings/page.tsx, src/lib/actions.ts</files>
  <read_first>src/app/dashboard/settings/page.tsx</read_first>
  <action>
    - Create `src/lib/actions.ts` exporting a `"use server"` function `saveSmtpSettings(formData: FormData)`. Inside, it reads `host`, `port`, `userString`, `passString`, gets the logged-in user via `getServerSession`, and `prisma.smtpSettings.upsert`. Ensure it runs `revalidatePath("/dashboard/settings")`.
    - Create `src/app/dashboard/settings/page.tsx` containing a basic Form that invokes `saveSmtpSettings`.
    - The Form should include 4 required input fields with labels: SMTP Host (e.g., smtp.gmail.com), Port (e.g., 465 or 587), Username (Email), Password (App Password).
    - At the top of `page.tsx`, fetch existing `SmtpSettings` and plug them into the form as `defaultValue` so the user knows they're saved.
  </action>
  <acceptance_criteria>
    - `src/lib/actions.ts` exports `saveSmtpSettings` modifying the db
    - `src/app/dashboard/settings/page.tsx` renders a form binding to `saveSmtpSettings`
  </acceptance_criteria>
</task>
