---
phase: 02-scraping-engine
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/app/dashboard/campaigns/page.tsx, src/app/dashboard/leads/page.tsx, src/components/ui/AutoRefresh.tsx]
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - Campaign UI shows real-time or polled updates on scraping status
  tests:
    - Build passes
---

## Objective
Connect the visual frontend Dashboard UI to the backend scraping statuses, providing user feedback on campaign progress.

## Tasks

<task type="auto">
  <name>Task 1: Add Auto-Refresh to Campaigns</name>
  <files>src/app/dashboard/campaigns/page.tsx, src/components/ui/AutoRefresh.tsx</files>
  <read_first>src/app/dashboard/campaigns/page.tsx</read_first>
  <action>
    - Create a simple Client Component `src/components/ui/AutoRefresh.tsx`: `"use client"; import { useEffect } from "react"; import { useRouter } from "next/navigation"; export function AutoRefresh({ interval = 10000 }) { const router = useRouter(); useEffect(() => { const timer = setInterval(() => router.refresh(), interval); return () => clearInterval(timer); }, [router, interval]); return null; }`.
    - Inside `src/app/dashboard/campaigns/page.tsx`, import `<AutoRefresh />` and place it at the top of the rendered DOM. This ensures the Server Component data (Prisma list) refreshes every 10 seconds to reflect status changes generated by the independent Scraper worker process.
  </action>
  <acceptance_criteria>
    - `src/components/ui/AutoRefresh.tsx` exists and implements `setInterval` with `router.refresh()`
    - `src/app/dashboard/campaigns/page.tsx` renders the `AutoRefresh` component
  </acceptance_criteria>
</task>
