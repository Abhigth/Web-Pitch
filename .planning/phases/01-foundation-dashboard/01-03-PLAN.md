---
phase: 01-foundation-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [src/app/dashboard/page.tsx, src/app/dashboard/campaigns/page.tsx, src/app/dashboard/leads/page.tsx, src/app/dashboard/templates/page.tsx, src/components/layout/DashboardLayout.tsx]
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]
must_haves:
  truths:
    - "User can view Replies metric on dashboard"
    - "User can create campaigns via a multi-step setup"
    - "User can view a table of leads"
    - "User can edit plain-text templates with variables"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Main dashboard metrics view"
    - path: "src/app/dashboard/campaigns/page.tsx"
      provides: "Campaign wizard and management"
    - path: "src/app/dashboard/templates/page.tsx"
      provides: "Plain-text template editor"
  key_links: []
---

<objective>
Implement the dashboard interface, campaign setup wizard, lead viewer, and plain-text template editor.

Purpose: Fulfills the remaining CORE features, providing the primary user interface for the lead generation engine.
Output: Fully functional dashboard layouts and data-entry forms.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-dashboard/01-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Layout and Metrics Page</name>
  <files>src/components/layout/DashboardLayout.tsx, src/app/dashboard/page.tsx</files>
  <read_first>.planning/phases/01-foundation-dashboard/01-CONTEXT.md, src/app/dashboard/page.tsx</read_first>
  <action>Create `DashboardLayout.tsx` with a sidebar navigation (Dashboard, Campaigns, Leads, Templates, Settings). In `src/app/dashboard/page.tsx`, create a metrics view highlighting "Replies" prominently as a stat card, along with "Active Leads" and "Sent Emails". Fetch this data using server components/Prisma.</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - src/components/layout/DashboardLayout.tsx contains navigation links
    - src/app/dashboard/page.tsx contains a stat card for "Replies"
  </acceptance_criteria>
  <done>Dashboard renders the sidebar and top-level KPI metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Campaign Setup Wizard</name>
  <files>src/app/dashboard/campaigns/page.tsx, src/components/campaigns/CampaignWizard.tsx</files>
  <read_first>prisma/schema.prisma, .planning/phases/01-foundation-dashboard/01-CONTEXT.md, src/app/dashboard/campaigns/page.tsx</read_first>
  <action>Create a `CampaignWizard.tsx` holding a multi-step form (Step 1: Details like name/location/keywords, Step 2: Review). On submit, it calls a Server Action to save the Campaign to the database. Wrap it in `src/app/dashboard/campaigns/page.tsx` with a list of existing campaigns.</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - CampaignWizard.tsx contains multiple distinct form steps/states
    - page.tsx lists campaigns from the database
  </acceptance_criteria>
  <done>User can create campaigns through a guided UI.</done>
</task>

<task type="auto">
  <name>Task 3: Lead Viewer</name>
  <files>src/app/dashboard/leads/page.tsx, src/components/leads/LeadTable.tsx</files>
  <read_first>prisma/schema.prisma, src/app/dashboard/leads/page.tsx</read_first>
  <action>Create `LeadTable.tsx` using Shadcn table or basic HTML table to display Leads (Name, Email, Website, Status). In `src/app/dashboard/leads/page.tsx`, fetch leads from Prisma and pass them to the table.</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - LeadTable.tsx renders HTML table elements or Shadcn Table
    - page.tsx fetches db.lead.findMany
  </acceptance_criteria>
  <done>Leads can be viewed in a tabulated format.</done>
</task>

<task type="auto">
  <name>Task 4: Template Editor</name>
  <files>src/app/dashboard/templates/page.tsx, src/components/templates/TemplateEditor.tsx</files>
  <read_first>.planning/phases/01-foundation-dashboard/01-CONTEXT.md, src/app/dashboard/templates/page.tsx</read_first>
  <action>Create `TemplateEditor.tsx` using a plain `<textarea>` (no WYSIWYG) taking up the main content area, with inputs for Subject Line and Type (PITCH/FOLLOWUP). Add instructions for variables like `{{BusinessName}}`. Let users save it to the DB via Server Actions. Add it to `src/app/dashboard/templates/page.tsx`.</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - TemplateEditor.tsx uses a textarea and highlights variable usage conceptually
    - page.tsx implements Server Actions or forms to save Template
  </acceptance_criteria>
  <done>Templates can be configured in plain-text.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All database models interact correctly with the UI Server Actions
</verification>

<success_criteria>
- All tasks completed
- Dashboard UI allows management of Campaigns, Leads, and Templates
- The entire foundation phase is usable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dashboard/01-03-SUMMARY.md`
</output>
