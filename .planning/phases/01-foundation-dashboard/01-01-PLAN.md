---
phase: 01-foundation-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, prisma/schema.prisma, src/lib/db.ts, components.json]
autonomous: true
requirements: [CORE-02, CORE-03, CORE-04]
must_haves:
  truths:
    - "Database schema can be pushed to a live database"
    - "Next.js project builds successfully"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Database models for User, Campaign, Lead, and Template"
    - path: "src/lib/db.ts"
      provides: "Prisma client singleton"
  key_links: []
---

<objective>
Initialize Next.js project core and establish PostgreSQL database schemas using Prisma.

Purpose: Provides the foundational web framework, styling setup, and data structures needed for authentication and dashboard features.
Output: Initialized Next.js project with Tailwind, Shadcn, and a comprehensive Prisma schema.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-dashboard/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js and Shadcn Setup</name>
  <files>package.json, components.json</files>
  <read_first>.planning/phases/01-foundation-dashboard/01-CONTEXT.md</read_first>
  <action>Ensure a Next.js App Router project is initialized with TypeScript, Tailwind CSS, and ESLint. If missing, create it in the project root. Initialize Shadcn UI using the CLI (`npx shadcn@latest init -d`). Add basic structural components (e.g. `button`, `card`, `input`, `label`).</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - package.json contains next, react, tailwindcss
    - components.json is created
  </acceptance_criteria>
  <done>Next.js setup is functional and builds correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma Database Schema setup</name>
  <files>prisma/schema.prisma, src/lib/db.ts</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Initialize Prisma (`npx prisma init`). In `schema.prisma` (datasource db provider = "postgresql"), define models for NextAuth and the app:
- User (id, email, name, emailVerified, image)
- Campaign (id, userId, name, location, keywords, status, createdAt)
- Lead (id, campaignId, name, website, email, phone, status, source)
- Template (id, userId, type (PITCH vs FOLLOWUP), subject, bodyPlain)
- Account, Session, VerificationToken (NextAuth requirements)
Create `src/lib/db.ts` exporting a singleton PrismaClient instance to prevent connections exhaustion in dev.</action>
  <verify>npx prisma validate passes</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains "model Campaign" and "model Lead"
    - src/lib/db.ts exports PrismaClient
  </acceptance_criteria>
  <done>Database schemas are fully defined and validated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npx prisma validate` passes
</verification>

<success_criteria>
- All tasks completed
- No errors or warnings introduced
- Next.js framework and ORM are prepared for feature development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dashboard/01-01-SUMMARY.md`
</output>
