---
phase: 01-foundation-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/app/api/auth/[...nextauth]/route.ts, src/lib/auth.ts, src/app/(auth)/login/page.tsx, src/middleware.ts]
autonomous: true
requirements: [CORE-01]
user_setup:
  - service: Resend or SMTP Provider
    why: "Magic link emails require an active SMTP transporter"
    env_vars:
      - name: EMAIL_SERVER
        source: "Your SMTP provider credentials"
      - name: EMAIL_FROM
        source: "The sender email for Magic Links"
must_haves:
  truths:
    - "Unauthenticated users cannot access dashboard pages"
    - "System can send magic link emails"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "NextAuth configuration"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login UI"
  key_links:
    - from: "src/app/api/auth/[...nextauth]/route.ts"
      to: "src/lib/auth.ts"
      via: "imports authOptions"
      pattern: "authOptions"
---

<objective>
Implement User Authentication using NextAuth and Magic Links.

Purpose: Secures the application and associates Campaigns and Templates to the logged-in User.
Output: Working authentication flow with a protected layout.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-dashboard/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NextAuth Configuration</name>
  <files>src/lib/auth.ts, src/app/api/auth/[...nextauth]/route.ts, src/middleware.ts</files>
  <read_first>.planning/phases/01-foundation-dashboard/01-CONTEXT.md, src/lib/db.ts</read_first>
  <action>Install `next-auth` and `@next-auth/prisma-adapter`. In `src/lib/auth.ts`, configure NextAuth with the Prisma adapter and the EmailProvider for magic links. Create the NextAuth API route handling GET/POST in `src/app/api/auth/[...nextauth]/route.ts`. Add a simple `src/middleware.ts` to protect routes under `/dashboard`.</action>
  <verify>Middleware successfully blocks unauthenticated access</verify>
  <acceptance_criteria>
    - src/lib/auth.ts contains EmailProvider configuration
    - src/middleware.ts exports a config protecting /dashboard
  </acceptance_criteria>
  <done>NextAuth is fully configured and protecting routes.</done>
</task>

<task type="auto">
  <name>Task 2: Login UI</name>
  <files>src/app/(auth)/login/page.tsx, src/components/auth/LoginForm.tsx</files>
  <read_first>.planning/phases/01-foundation-dashboard/01-CONTEXT.md</read_first>
  <action>Create a clean, minimal login page at `/login`. Implement `LoginForm.tsx` using a Shadcn card, text input for email, and a submit button. It will call `signIn('email', { email })` on submit. Display a success message once the magic link is sent.</action>
  <verify>npm run build completes without errors</verify>
  <acceptance_criteria>
    - page.tsx uses the LoginForm component
    - LoginForm.tsx contains a call to signIn('email')
  </acceptance_criteria>
  <done>Login UI provides a smooth magic link request flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] NextAuth types conform correctly
</verification>

<success_criteria>
- All tasks completed
- Protected routes require authentication
- Magic link login requests can be submitted via the UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dashboard/01-02-SUMMARY.md`
</output>
